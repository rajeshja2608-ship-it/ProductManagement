name: Product Management CD

on:
  workflow_run:
    workflows:
      - Product Management CI
    types:
      - completed

jobs:

  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    runs-on: self-hosted

    steps:

      - name: Download deployment artifact
        uses: actions/download-artifact@v4
        with:
          name: ProductManagement-Publish
          path: ./publish

      - name: Deploy API
        shell: powershell
        run: |
          # Stop API IIS site
          # Copy ./publish/API to C:\IIS\Products\API
          # Start API IIS site

      - name: Deploy UI
        shell: powershell
        run: |
          # Stop UI IIS site
          # Copy ./publish/UI to C:\IIS\Products\UI
          # Start UI IIS site

      - name: Verify deployment
        shell: powershell
        run: |
          # Test API
          # Test UI
